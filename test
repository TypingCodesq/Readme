-- IMPORTANTE: Este script DEBE estar en ServerScriptService
-- NO funcionará en StarterPlayerScripts, StarterGui, o como LocalScript

-- Verificar si HTTP está habilitado
local HttpService = game:GetService("HttpService")

if not HttpService.HttpEnabled then
    error("HTTP requests are not enabled! Go to Game Settings > Security > Allow HTTP Requests")
end

-- Función para obtener la IP del servidor
local function getServerIP()
    print("Intentando obtener IP del servidor...")
    
    local success, result = pcall(function()
        local response = HttpService:GetAsync("http://ip-api.com/json")
        local data = HttpService:JSONDecode(response)
        return data.query
    end)
    
    if success then
        print("✅ IP del servidor:", result)
        return result
    else
        warn("❌ Error al obtener IP:", result)
        warn("Posibles causas:")
        warn("1. HTTP Requests no están habilitados en Game Settings")
        warn("2. El script no está en ServerScriptService")
        warn("3. Problemas de conexión")
        return nil
    end
end

-- Ejecutar
local ipAddress = getServerIP()

if ipAddress then
    print("🌐 IP obtenida exitosamente:", ipAddress)
else
    print("💥 No se pudo obtener la IP")
end
